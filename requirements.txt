# Core dependencies
pydantic>=2.0,<3.0
pyyaml>=6.0
numpy>=1.24
tqdm>=4.65

# Retrieval / embeddings (lightweight local option)
scikit-learn>=1.3
rank_bm25>=0.2
sentence-transformers>=2.2
faiss-cpu>=1.7

# LLM clients (optional; install as needed)
# openai>=1.0
# google-generativeai>=0.5

# Testing
pytest>=7.4
pytest-cov>=4.1

# Linting / formatting (dev)
ruff>=0.4
mypy>=1.8
